environment {
  GITHUB_TOKEN = credentials(lartiorkor-gh-creds)
}

pipeline {
  agent any
  tools {nodejs "nodejs"}

  stages {
    
    stage('Release') {
        when {
            branch 'main';
        }
        steps {
          sh 'npm i'
          sh 'npm run release'
        }
    }
  }
}
